spring:
  datasource:
    url: jdbc:h2:mem:groupdocs_db;DATABASE_TO_UPPER=false;DB_CLOSE_DELAY=-1
    username: groupdocs
    password: groupdocs
  jpa:
    properties:
      hibernate.dialect: org.hibernate.dialect.H2Dialect
    database-platform: org.hibernate.dialect.H2Dialect
    hibernate:
      ddl-auto: create